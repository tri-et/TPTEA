export const getOrderHistory = state => {
  return state.recsOrderHistory
}

export const getMenuCategory = state => {
  return state.recsMenuCategory
}

export const getSubMenu = state => {
  return state.recsSubMenu
}

export const getRecsOrdered = state => {
  return state.recsOrdered
}
